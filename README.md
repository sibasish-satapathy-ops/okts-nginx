# ProxyServer 
This is proxy server of nginx.
If you want to make proxy then go in default.conf, Here edit the file & build & run the docker image.
for testing purpose your image is run the '< your local id address:port no >/go'
